name: pr

on:
  pull_request:
    branches:
      - main
    paths-ignore:
      - "**/*.md"
      - "**/*.gitignore"
      - "työtuntikirjanpito.md"
      - "backend/Makefile"

jobs:
  test:
    uses: ./.github/workflows/tests.yaml
    with:
      go-version: 1.26.0 # env.GO_VERSION fails here. idk why
